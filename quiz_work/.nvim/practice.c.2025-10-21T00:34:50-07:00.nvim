#define _POSIX_C_SOURCE 200809L
#include <string.h>
#include <stdio.h>

int main(void) {
  
  char *saveptr, *token;
  token = strtok_r("gcc main.c -o main", " \n", &saveptr);

  while (token != NULL) {
    
    printf("Token: %s\n", token);
    token = strtok_r(NULL, " \n", &saveptr);

  }
    
}
